# 📊 Data

This directory is the designated home for project datasets. To keep the repository lightweight, large data files are **excluded from version control** via `.gitignore`.

## Sources
* **Raw data:** [Link to data location]


## Expected Structure
If you are reproducing this study, please organize the data as follows:
- `data/raw/`: Original files as downloaded.
- `data/processed/`: Intermediate files generated by `scripts/preprocess.py`.
